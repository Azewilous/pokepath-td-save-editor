/** A single entry from pokedex.json */
export interface PokedexEntry {
  id: string;
  name: string;
  ability: string;
  acquisition: string;
  critChance: string;
  range: string;
  attackSpeed: string;
  damage: string;
  dps: string;
}
